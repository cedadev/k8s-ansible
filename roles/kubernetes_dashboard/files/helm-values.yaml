fullnameOverride: kubernetes-dashboard

ingress:
  enabled: false

rbac:
  create: true
  clusterAdminRole: true

nodeSelector:
  role: infrastructure

tolerations:
  - key: role
    operator: Equal
    value: infrastructure
    effect: NoSchedule
