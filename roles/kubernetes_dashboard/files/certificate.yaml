apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: kubernetes-dashboard
spec:
  secretName: kubernetes-dashboard-tls
  issuerRef:
    name: default-issuer
    kind: ClusterIssuer
  commonName: "*"
