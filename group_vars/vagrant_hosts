setup_networking: true
# Ceph installation is currently broken for CentOS, so just switch it off for now
storage_rook: "{{ ansible_distribution == 'Ubuntu' }}"

# root_volume_group: vagrant-vg

cluster_interface: enp0s8
use_userspace_proxy: true

artifact_namespace: vagrant
